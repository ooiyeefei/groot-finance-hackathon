# Quickstart: e-Invoice Schema Changes

**Branch**: `016-e-invoice-schema-change` | **Date**: 2026-02-19

## Implementation Order

The changes must be applied in this sequence to avoid build errors:

### Step 1: Add Status Constants

**File**: `src/lib/constants/statuses.ts`

Add `LHDN_STATUSES`, `PEPPOL_STATUSES`, and `EINVOICE_TYPES` constant objects with their types and values arrays. Follow the existing pattern (e.g., `SALES_INVOICE_STATUSES`).

### Step 2: Add Convex Validators

**File**: `convex/lib/validators.ts`

Import the new `*_VALUES` arrays from statuses.ts. Create 3 new validators using the existing `literalUnion()` helper:
- `lhdnStatusValidator`
- `peppolStatusValidator`
- `einvoiceTypeValidator`

### Step 3: Extend Convex Schema

**File**: `convex/schema.ts`

1. Import the 3 new validators
2. Add 15 fields to `sales_invoices` table (LHDN tracking, Peppol tracking, einvoiceType)
3. Add 9 fields to `customerSnapshot` embedded object (tin, brn, structured address)
4. Add 7 fields to `businesses` table (MSIC, SST, TIN, BRN, Peppol, LHDN client ID)
5. Add 10 fields to `customers` table (TIN, BRN, SST, Peppol, structured address)
6. Add 3 new indexes

### Step 4: Extend TypeScript Types

**File**: `src/domains/sales-invoices/types/index.ts`

1. Extend `CustomerSnapshot` interface with 9 new optional fields
2. Extend `customerSnapshotSchema` Zod schema to match

### Step 5: Extend Snapshot Builder

**File**: `src/domains/sales-invoices/components/customer-selector.tsx`

Update `handleSelectCustomer` function (line ~85) to copy new fields from customer to snapshot.

### Step 6: Extend Convex Mutations

**File**: `convex/functions/salesInvoices.ts`

1. Extend `create` mutation customerSnapshot arg validation
2. Extend `update` mutation customerSnapshot arg validation
3. Extend `send` mutation auto-customer creation to map new snapshot fields

### Step 7: Build & Deploy

```bash
npm run build                    # Must pass
npx convex deploy --yes          # Deploy schema to production
```

## Verification Checklist

- [ ] `npm run build` passes with zero errors
- [ ] `npx convex deploy --yes` succeeds
- [ ] Existing sales invoices display correctly (no regression)
- [ ] New customer record can be created with TIN, BRN, structured address
- [ ] New sales invoice captures extended customer snapshot
- [ ] TypeScript types auto-generated by Convex match new schema
