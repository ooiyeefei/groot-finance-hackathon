# Document Processor Lambda - Python 3.11 Container Image
# AWS Durable Execution SDK for fault-tolerant workflows
# Note: Container images support ANY Python version with durable SDK

# AWS Durable Execution SDK (works with container images on any Python version)
aws-durable-execution-sdk-python

# Pin numpy for stability (pre-built wheels for Python 3.11)
numpy==1.26.4

# DSPy for structured LLM extraction
dspy>=2.6.0

# Pydantic for data validation
pydantic>=2.0.0,<3.0.0

# Image processing
Pillow>=10.0.0,<12.0.0

# PDF to image conversion (requires Poppler in Lambda Layer)
pdf2image>=1.16.0

# HTTP client for Convex and Gemini API calls
httpx>=0.25.0,<1.0.0

# AWS SDK (usually included in Lambda, but pin for consistency)
boto3>=1.34.0

# LiteLLM for Gemini integration (used by DSPy)
litellm>=1.40.0,<2.0.0

# Sentry for error tracking
sentry-sdk>=2.0.0

# QR code detection from receipt images (019-lhdn-einv-flow-2)
# pyzbar: fast QR/barcode decoder (requires libzbar.so — copied from AL2 EPEL in Dockerfile)
pyzbar==0.1.9
# OpenCV for image pre-processing to improve QR decode reliability
opencv-python-headless==4.10.0.84
