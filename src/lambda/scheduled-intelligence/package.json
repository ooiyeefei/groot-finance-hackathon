{
  "name": "finanseal-scheduled-intelligence",
  "version": "1.0.0",
  "description": "Node.js Lambda dispatcher for scheduled intelligence jobs (EventBridge → Lambda → Convex HTTP API)",
  "main": "index.ts",
  "scripts": {
    "build": "tsc",
    "lint": "eslint .",
    "test": "jest"
  },
  "dependencies": {
    "@aws-sdk/client-lambda": "^3.700.0",
    "@aws-sdk/client-ssm": "^3.700.0",
    "node-fetch": "^3.3.2"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.145",
    "@types/node": "^20.16.15",
    "typescript": "^5.9.3"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
